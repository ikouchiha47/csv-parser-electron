{
  "name": "csv-parser-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "electron main.js",
    "flow": "flow; test $? -eq 0 -o $? -eq 2",
    "compile-src": "babel src -d build",
    "compile-front": "browserify --ignore-missing --no-builtins --no-commondir --insert-global-vars=\"global\" --no-browser-field -t [ babelify ] scripts/src/app.js -o scripts/out/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "csv": "^1.1.0",
    "electron-prebuilt": "^0.35.2",
    "react": "^15.3.2",
    "react-dom": "^15.3.2"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-plugin-transform-flow-strip-types": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-react": "^6.16.0",
    "babelify": "^7.3.0",
    "flow-bin": "^0.35.0"
  }
}
